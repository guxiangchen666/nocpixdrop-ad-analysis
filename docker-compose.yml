services:
  ad-analysis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nocpixdrop-ad-analysis
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "${APP_PORT:-3010}:3000"
